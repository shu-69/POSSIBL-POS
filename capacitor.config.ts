import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.possibl.pos',
  appName: 'POSSIBL POS',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
